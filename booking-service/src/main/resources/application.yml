spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/apextick
  application:
    name: booking-service
  datasource:
    url: jdbc:postgresql://localhost:5440/apextick
    username: postgres
    password: eta8827
  jpa:
    open-in-view: false        
    hibernate:
      ddl-auto: validate      
    show-sql: true
    properties:
      hibernate.format_sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  rabbitmq:
    host: localhost
    port: 5672
    username: apextick
    password: apextick

  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

server:
  port: 8081